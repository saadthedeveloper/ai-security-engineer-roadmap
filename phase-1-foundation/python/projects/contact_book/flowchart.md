[Show Menu] → [User picks option]
   ↓
  1. Add Contact → (ask details → add to list → back to menu)
  2. View All → (print list → back to menu)
  3. Search → (get query → show results → back to menu)
  4. Delete → (choose contact → remove → back to menu)
  5. Exit → (save to file → goodbye)
   ↑
   └─── loop back to menu
