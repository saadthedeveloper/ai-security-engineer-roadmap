The spread operator is used to unpack elements/properties from an iterable or object into another array, object, or function call. Rest, on the other hand, is used to collect the remaining elements or properties into a single array or object. Spread always creates a shallow copy meaning; top‑level primitives are independent, nested references are shared. For true independence, use structuredClone() or another deep‑copy method.
